import 'package:flutter/material.dart';

import 'config.dart';

class CustomTheme {
  static ThemeData get lightTheme {
    return ThemeData(
      primaryColor: kBackgroundColor,
      fontFamily: 'Muli',
    );
  }
}
